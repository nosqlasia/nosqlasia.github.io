baseurl = "http://nosqlasia.github.io/"
title = "NoSQL Asia"
languageCode = "en-us"
disqusShortname = "nosqlasia" # Optional, enable Disqus integration
MetaDataFormat = "yaml"

[author]
    name = "NoSQL Asia"

[permalinks]
    # Optional. Change the permalink format for the 'post' content type.
    # The format shown here is the same one Jekyll/Octopress uses by default.
    # post = "/blog/:year/:month/:day/:title/"
    post = "/blog/:title/"

[indexes]
    # Optional. Use if you want tags and lists.
    category = "categories"

#
# All parameters below here are optional and can be mixed and matched.
#
[params]
    # Used when a given page doesn't set its own.
    defaultDescription = "NoSQL Events and Communities in South East Asia"
    defaultKeywords = "nosqlasia,nosql,mongodb,redis,couchbase,influxdb"

    # Changes sidebar background and link/accent colours.
    # See below for more colour options.
    # This also works: "theme-base-08 layout-reverse", or just "layout-reverse".
    theme = "theme-base-08"

    # Select a syntax highight.
    # Check the static/css/highlight directory for options.
    highlight = "sunburst"

    # Displays under the author name in the sidebar, keep it short.
    tagline = "Events and communities in South East Asia"

    # Metadata used to drive integrations.
    googleAuthorship = "Your Google+ profile ID"
    googleAnalytics = "UA-58840307-1"
    gravatarHash = "d0e16a7a5865cc10d8d9d22874ab9a85"

    # Sidebar social links, avoid enabling too many of these if possible.
    # These should be full URLs.
    github = "https://github.com/nosqlasia/"
    linkedin = ""
    googleplus = ""
    facebook = "https://www.facebook.com/groups/nosqlkl/"
    twitter = "http://twitter.com/nosqlasia"
